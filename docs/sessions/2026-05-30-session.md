# Session: 2026-05-30
**Project:** DockLock (Vigil Screen)
**Duration:** ~30 min

## What was done
- Reviewed current project status (branch, version, commits, dirty files)
- Created PR #12: `feature/v0.4.0-presentation-mode` → `main` with full description and test checklist
- Merged `main` into `feature/v0.4.0-presentation-mode` — resolved one conflict in `PanicModeManager.swift`
- Committed and pushed `conventions.md` frontmatter paths filter (`**/*.swift`)

## Current state
`feature/v0.4.0-presentation-mode` is fully up to date with `main`. PR #12 is open and ready to review/merge. No uncommitted changes. Clean working tree.

## In progress / next action
- [ ] Merge PR #12 (`feature/v0.4.0-presentation-mode` → `main`) on GitHub
- [ ] Archive and upload v0.4.0 to App Store Connect
- [ ] Record 30-second Panic Mode demo video (highest ROI per PLAN.md)

## Blockers
- None

## Open decisions
- v0.5.0 scope: Profiles/Modes vs multi-device Bluetooth pairing (deferred per PLAN.md)

## Commits this session
```
aa0aa2d chore: scope conventions.md to Swift files via frontmatter paths filter
9f8cb59 merge: pull main into feature/v0.4.0-presentation-mode
```

## Notes
- Conflict resolution in `PanicModeManager.swift`: kept both `keyboardEventTap` (v0.4.0, Cmd+Tab blocking) and `clickEventTapSource` (main, run loop cleanup); used main's `guard let refcon` callback structure so `mgr` stays in scope for auto-merged `preemptiveHoleUpdate` call.
- Memory note: DockLock lives at `~/Work/Apple/DockLock/`, not `~/Work/DockLock/` — update project_structure.md.
